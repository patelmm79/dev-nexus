# Configuration Directory

This directory contains configuration templates and files for the A2A server.

## Files

- `agent_card_template.json` - Template for the AgentCard published at `/.well-known/agent.json`
- `secrets.yaml.example` - Example secrets configuration for local development

## Usage

These files are included in the Docker image and used by the A2A server at runtime.
